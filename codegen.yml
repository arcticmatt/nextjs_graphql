overwrite: true
schema: "http://localhost:4000/graphql"
documents: "graphql/**/*.ts"
generates:
  ./types/graphqlGen.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
